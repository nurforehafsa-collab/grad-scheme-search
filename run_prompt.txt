Weekly grad scheme / placement search for a part-time MSc Building Surveying student (heritage focus, wants to work with historical buildings/structures). Repo root has tracker.json, README.md, summary.md, dashboard_template.html — read README.md first for the full fit rules and schema, they're authoritative.

Do, in order:

1. Read tracker.json for current entries.

2. Check these sources (public listing pages only, no login, this is a once-a-week check so stay light — a handful of fetches/searches per source, not exhaustive crawling):
   Track 1 (building surveying): Turner & Townsend early careers/graduate page, AtkinsRéalis early careers page, CBRE UK graduate programme page, Frankham careers page, Gradcracker (gradcracker.com) filtered to building surveying/construction/built environment, RICS Recruit (ricsrecruit.com) filtered to building surveying / graduate/trainee level.
   Track 2 (heritage building conservation, London): CharityJob (charityjob.co.uk) searches for heritage building surveyor, historic building conservation officer/consultant, building conservation coordinator; RICS Recruit/Gradcracker also checked for "heritage"/"conservation" building surveying roles; CultureJobs/Arts Council-adjacent boards filtered to heritage building conservation only (not general arts/culture/events); Civil Service Fast Stream official page (application window/deadline only, not general vacancies).

3. For each posting found, extract role title, employer, deadline, link, and start date if stated.

4. Apply the fit rules from README.md before adding anything — Track 1 vs Track 2 criteria, and the hard excludes:
   - 5+ years' experience required, animal care, fundraising-heavy, commission-only 'brand ambassador'/street marketing roles dressed up as coordinator titles, pure-QS roles with no building surveying pathway, new-build/investment-only schemes with no heritage angle.
   - Requires an already-completed RICS-accredited degree with no part-qualified/currently-studying route.
   - Start date must be compatible with an August/September 2027 start (Hafsa's MSc runs to Aug 2027) — 2027-cohort graduate schemes or rolling/APC-support roles that could accommodate an Aug/Sept 2027 joiner are fine; roles explicitly requiring an earlier 2026/early-2027 start are excluded even if otherwise strong. Flag rather than drop if start date isn't stated.
   - Non-London/non-Kingston-commutable location for immediate-start Track 1 roles — except 2027-cohort graduate schemes, which are fine regardless of location since they start after the MSc finishes.
   - Track 2 must be building/structure-focused: heritage building surveying, historic building conservation, condition assessment of listed/historic structures. Generic heritage community engagement, visitor experience, events, or programme administration roles are excluded even at a genuinely historic site, unless there's a clear hands-on historic-building-fabric component.

5. Dedupe against existing tracker.json entries by employer + name — never create duplicate rows on a re-run. For an existing entry whose deadline changed, update the deadline and add a short note about the change. Never overwrite or revert an entry's status field (e.g. leave 'applied' as 'applied') — only status-untouched fields (deadline, notes, link) should change on existing entries.

6. For genuinely new postings: generate a unique id (e.g. 'e-' + timestamp or similar), set status to 'not_started', and match the schema exactly: id, name, employer, track ('1'|'2'), status, deadline (YYYY-MM-DD or ''), link, notes.

7. Write the updated entries back to tracker.json, preserving any top-level fields you don't recognize (e.g. dashboard_url).

8. Rewrite summary.md with a 'what's new this week' report: new postings found (with a one-line fit reason each), deadline changes on existing entries, anything excluded and why, and any source whose page structure couldn't be parsed this run (note it explicitly rather than failing silently).

9. Commit and push: git add -A && git commit -m "Weekly grad scheme search update" && git push, back to main.

10. Dashboard artifact: open dashboard_template.html in the repo — the original hand-designed tracker board (dark blueprint background, card layout, status stamp circles, brass/rust accent colors). Build a new static, read-only HTML page reusing that same CSS almost verbatim, populated from the tracker.json entries you just wrote, grouped into Track 1/Track 2 sections sorted by deadline. If an Artifact-publishing tool is available in this session, publish it (check tracker.json's top-level dashboard_url field — reuse it if present so this updates the same page rather than minting a new one each week; if absent, publish fresh and write the returned URL back into tracker.json before committing). If no such tool is available in this session, just write the page to dashboard.html in the repo and note in summary.md that it wasn't published as a live artifact this run.

11. Notify: if a push-notification tool is available, send one with a short highlight (e.g. count of new postings or most urgent deadline) plus the dashboard URL/location. If unavailable, skip and note it in summary.md.

Treat this as a shortlist for human review, not an auto-apply pipeline — when in doubt about fit, include the entry but flag the uncertainty in its notes rather than silently dropping it.
